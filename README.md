# rss-reader

# RSS Reader

A simple Python RSS feed reader that fetches and displays the latest articles from a given RSS URL.

## Features
- Fetches live RSS feeds
- Displays title, link, and description
- Shows top 5 latest articles

## How to Run

1. Install dependencies:
   pip install -r requirements.txt

2. Run:
   python3 rss_reader.py
